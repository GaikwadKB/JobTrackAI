<?xml version="1.0" encoding="utf-8"?>
<!--
    Mirrors backup_rules.xml for Android 12+'s split cloud-backup /
    device-transfer extraction model. Excludes everything by default;
    revisit per-file once the Room DB and DataStore files exist (Phases 7+)
    if selective, encrypted transfer is desired.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
